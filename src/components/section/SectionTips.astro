---
interface Props {
  sectionName: string;
}
const { sectionName } = Astro.props;
const Icon = `<svg class="h-6 w-6 text-green-600 mr-3 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>`;
---
<section class="bg-green-50 py-12 md:py-16">
  <div class="container mx-auto px-4 max-w-4xl">
    <h2 class="text-2xl md:text-3xl font-bold text-green-800 mb-8 text-center">
      Conseils Plomberie pour les Résidents de {sectionName}
    </h2>
     <div class="space-y-5">
      <div class="flex items-start bg-white p-4 rounded-lg shadow border border-green-200">
        <Fragment set:html={Icon} />
        <div>
          <h3 class="font-semibold text-lg text-green-700 mb-1">Prévention Spécifique au Quartier</h3>
          <p class="text-gray-700 text-sm">
            En raison de [particularité locale de {sectionName}], nous recommandons aux habitants de {sectionName} de [conseil préventif spécifique pour {sectionName}] pour éviter les problèmes récurrents de plomberie.
             {/* Developer Note: Replace bracketed text with actual data */}
          </p>
        </div>
      </div>
      <div class="flex items-start bg-white p-4 rounded-lg shadow border border-green-200">
        <Fragment set:html={Icon} />
        <div>
          <h3 class="font-semibold text-lg text-green-700 mb-1">Entretien Régulier</h3>
          <p class="text-gray-700 text-sm">
            Pour les habitations de {sectionName}, un entretien préventif de vos canalisations tous les [période recommandée pour {sectionName}, ex: 18 mois] permet d'éviter la plupart des problèmes de bouchons et d'écoulement.
             {/* Developer Note: Replace bracketed text with actual data */}
          </p>
        </div>
      </div>
    </div>
  </div>
</section>